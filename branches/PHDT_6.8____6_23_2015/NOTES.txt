% New for this Release -- 6.7

pushed performance requirements up to the vehcle parameters initialization script

made a folder for the old FLUX scripts

changed DP function to fail it when the engine applies torque when it is off

changed DP to get ride of nmerical interpolation issues that were failing it, the engine was off and there was 0.24 Nm of torque applied
this is not a reasonable reason to fail it, so it was move to 0 Nm if it was less than 3 Nm with the engine off

state and control grids were added for engine control

